1) What is Function Calling?
    it is to transform a humman text to a function Call.(calling the right function & gave it the right args).

2) Why is This Important? (the Function Call)
    it hellp to use othere system to not do everything by your self just call an API, or ask for a query, and to provide structured output using JSON of XML

3) The Challenge?
        Constrained decoding.

4) norm

5) make file

6) two folders : 

    * data/input/ : contain two file , 1) function_calling_tests.json 2) functions_definition.json
    *  data/output/

7) the constrainde decoder check the JSON format and the Valide Schema.

8) 



Parsing -> Pipeline (no constrainde decoding) -> Constrained decoding -> Machine State.
